#include "lists.h"

/**
 * free_listint - free location taken
 * @head: pointer to function
 * Return: nothing
 */
void free_listint(listint_t *head)
{
	listint_t *temp;

	while (head != NULL)
	{
		temp = head->next;
		free(head);
		head = temp;
	}
}
